resources:
  - repo: self
variables:
  dockerRegistryServiceConnection: 'ACR20rsms100prod-1'
  imageRepository: 'musicstore-products-microservice'
  containerRegistry: 'acr20rsms100prod.azurecr.io'
  dockerfilePath: '$(Build.SourcesDirectory)/MusicStore.Microservices.Products.Api/Dockerfile'
  tag: '$(Build.BuildId)'
  vmImageName: 'ubuntu-latest'
trigger:
  branches:
    include:
      - main
      - develop
  paths:
    include:
      - MusicStore.Products.Api
      - MusicStore.Products.Business
      - MusicStore.Products.Data
      - MusicStore.Products.Events

stages:
- stage: Build
  displayName: Build and push stage
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: $(vmImageName)
    steps:
    - task: Docker@2
      displayName: Build and push an image to container registry
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: $(dockerfilePath)
        containerRegistry: $(dockerRegistryServiceConnection)
        buildContext: '.'
        tags: |
          $(tag)